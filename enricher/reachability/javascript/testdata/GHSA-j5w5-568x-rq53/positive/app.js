const { extractSignals } = require('@evomap/evolver/src/gep/signals');

extractSignals({
  recentSessionTranscript: '',
  todayLog: '',
  memorySnippet: '',
  userSnippet: '',
  recentEvents: [],
});
