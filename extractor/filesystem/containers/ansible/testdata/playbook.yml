- hosts: webservers
  tasks:
    - name: Start nginx
      docker_container:
        name: my_nginx
        image: nginx:1.25
    - name: Start redis
      docker_container:
        name: my_redis
        image: redis:7.0
