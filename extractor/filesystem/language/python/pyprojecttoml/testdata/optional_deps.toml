[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "my-package"
version = "1.0.0"
dependencies = [
    "requests==2.32.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "black==23.1.0",
]
async = [
    "aiohttp>=3.8.0",
]
